imageviewer.tests package
=========================

The tests were created using the ``unittest`` framework. To run them open a command window and cd into the
`imageviewer/tests` directory, then type::

    python -m unittest test.py

to run all tests. If you want to run single tests instead of all, you can specify the command like this::

    python -m unittest test.TestImageViewer

to run the tests of the ``TestImageViewer`` class.


test module
-----------------------------

.. automodule:: imageviewer.tests.test
   :members:
   :undoc-members:
   :show-inheritance:
